import { MessageTemplatesAdmin } from "@/components/config/MessageTemplatesAdmin";

export const metadata = {
  title: "Templates messages — Parametrage",
};

export default function MessageTemplatesPage() {
  return <MessageTemplatesAdmin />;
}
